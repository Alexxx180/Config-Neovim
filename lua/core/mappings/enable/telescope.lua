telescope = {
    builtin = { 
        help_tags, keymaps, find_files, builtin, grep_string,
        live_grep, diagnostics, resume, oldfiles, buffers,
    },
    buffer = { buffer_search, live_grep, find_files, }
}
